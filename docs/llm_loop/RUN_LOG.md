# LLM 품질 루프 실행 기록

이 파일은 실제로 수행한 작업만 시간순으로 기록한다. 계획은 `PLAN_3DAYS.md`, 현재 상태는
`STATUS.md`, 점수는 `SCOREBOARD.md`를 기준으로 본다.

## 2026-08-17

### 22:07 KST — 작업 시작

- 저장소 `C:\National_Assembly_3`, 브랜치 `main`, HEAD `779a12a` 확인
- 기존 미추적 `.superpowers/`, `docs/images/` 확인 후 보존
- 현재 기본 답변 모델 `gpt-5.6-terra`, 추론 강도 `low` 기록
- 현재 기본 재순위 모델 `gpt-5.6-luna`, 추론 강도 `low` 기록

### 22:08 KST — 기록 구조 생성

- `docs/llm_loop`와 `data/llm_loop_v2` 생성
- 3일 계획, 상태판, 점수판, 결정 기록 생성

### 22:12 KST — 신규 평가 스키마 완료

- 기존 평가 코드와 분리된 `schema.json` 및 표준 라이브러리 검증기 구현
- 정답 가능/불가능 조건, 근거 필수, 중복 질문, split 일치 규칙 구현
- 최초 단위 테스트 4개 PASS

### 22:14 KST — 원문 후보 추출

- 기존 `data/eval`을 읽지 않고 `data/v1/chunks`만 사용
- 적격 원문 청크 49,226개에서 seed `20260817`로 개발 후보 40개 추출
- 후보 SHA-256 `6f8892743931c8c399ea8beca8c9e931a2399d1aaa7904dd9743139418e1d948`

### 22:17 KST — 개발문제 20개 완료

- 후보 원문을 직접 검토해 신규 개발문제·정답·필수 주장·금지 주장·정확 값 작성
- `dev.jsonl` 20개 스키마 검증 PASS
- SHA-256 `9b3492c42fa20caacfb24768c27c44e55e7ec6cba9311961b8b89c81e698bc76`

### 22:19 KST — 실행환경과 DB 복구

- `backend/.venv` 생성 및 `backend/requirements.txt` 고정 버전 설치
- Docker 컨테이너 `national-assembly-db` 실행 확인
- `/health`: DB `ok`, chunks 420,410, embeddings 419,422

### 22:22 KST — 기준점 실행 보류

- 신규 실행기 `run_baseline.py` 구현
- `/query`는 질문과 검색된 근거를 OpenAI API로 전송하므로 사용자 명시 승인 전에 실행하지 않음
- 상태를 `NEW-003 BLOCKED`로 기록

### 22:24 KST — 블라인드 준비와 오프라인 검증 강화

- 개발문제의 20개 회의를 모두 제외하고 seed `20260818`로 원문 후보 260개 추출
- 후보 SHA-256 `53911c3c17f5abea69fbe392252e7e32c12dfd4f4c72964c9ea6e9ab99abbf5c`
- 개발/블라인드/예비 source 누수 검사 및 데이터 동결 도구 구현
- 신규 오프라인 단위 테스트 총 8개 PASS

### 22:27 KST — 블라인드 후보 품질 경보

- 원문 후보 260개의 위원회·역할 분포와 휴리스틱 경보 보고서 생성
- 문맥 의존 표현, 진행 발언, 마이크 중단, 선서문 가능성이 있는 후보 127개 표시
- 자동 통과시키지 않고 `NEW-005` 수동 검토 항목으로 등록

### 22:30 KST — 답변 불가 대조군 10개 생성

- 실제 회의가 존재하지만 지정 발언자가 그 위원회·날짜에 발언하지 않은 조합을 로컬 DB에서 선택
- 각 조합의 대상 회의 청크 수, 발언자 부재 0건, 다른 회의 발언 이력을 별도 증명 파일에 저장
- `blind-091`부터 `blind-100`까지 10개 초안 스키마 검증 PASS
- 아직 전체 블라인드 세트를 동결하지 않았으므로 상태는 `source_checked` 유지

### 22:34 KST — 전체 회귀 테스트

- 외부 API 호출을 막기 위해 `OPENAI_API_KEY`를 빈 값, 재순위를 비활성화한 테스트 프로세스로 실행
- 기존 테스트와 신규 테스트 합계 171개 PASS, 경고 2개
- 새 품질 루프 파일 추가로 인한 기존 기능 회귀는 발견되지 않음

### 22:35 KST — 첫 기준점 실행 무효 처리

- 위원회 정식명칭을 DB 약칭 필터 자리에 보내 첫 5개가 검색 `NONE`
- 운영용 분당 제한 때문에 이후 15개가 HTTP 429
- 결과를 삭제하지 않고 `baseline-v2-invalid` 실패 실행으로 보존, 점수에서는 제외

### 22:38 KST — 필터 수정 후 기준점 20개 완료

- 원문 표시용 정식명칭과 API 검색용 약칭을 데이터 구조에서 분리
- 운영용 분당 제한만 끈 평가 프로세스에서 20개 모두 HTTP 200
- 기존 시스템 등급 FULL 14, PARTIAL 6
- 원본 답변·인용·모델 사용량·비용·지연을 `runs/baseline-v2-valid`에 저장
- 1차 규칙은 명백한 실패만 차단하고 19개를 의미 검토 대상으로 보류

### 22:42 KST — 의미 채점 입력 준비

- 실제로 인용된 청크 32개의 전문을 로컬 DB에서 결합
- dev-012에서 인용 전문에 없는 세부 예산 금액을 답변이 추가한 의심 사례 확인
- 정답표와 인용 전문의 별도 채점 모델 전송은 추가 사용자 승인 전까지 중지

## 2026-08-18

### 00:19 KST — 독립 의미 채점 시작

- 사용자에게 정답표·인용 전문 전송과 API 10달러 한도 명시 승인 받음
- `gpt-5.6-sol`, reasoning `high`로 먼저 1개 형식 검증 후 나머지 재개
- 20개 모두 저장 완료: PASS 15, FAIL 5
- 의미 채점 사용량 입력 23,553 / 출력 7,999 토큰

### 00:45 KST — 실패 5개 수동 재검토

- `dev-002`, `dev-011`, `dev-019`는 질문보다 필수 정답이 넓은 루브릭 결함으로 확정
- 위 3개는 시스템 성능 점수에서 제외하되 원본 결과는 보존
- `dev-012`, `dev-016`은 인용 전문이 지지하지 않는 추가 주장으로 시스템 실패 확정
- 특히 `dev-012`는 기존 grounding `FULL`과 실제 인용 지지가 충돌

### 00:50 KST — 보수적 기준선 확정

- 규칙·의미 채점·수동 판정을 합치는 `summarize_baseline.py` 구현
- 집계기 단위 테스트 2개 PASS
- 유효 17개 중 15개 통과(88.2%), 근거 없는 추가 주장 2개로 최종 게이트 FAIL
- `final_scores.jsonl`, `baseline_summary.json`, `baseline_report.md` 저장

### 00:55 KST — 치명적 숫자·인물 검증과 1회 재생성 구현

- 답변 숫자를 문장에 직접 연결된 인용 전문과 대조하는 `unsupported_number` 추가
- 인용에 없는 명시 실명+직함을 차단하는 `unsupported_named_entity` 추가
- 치명적 실패면 주변 문맥을 제거한 본문만으로 high 추론 재생성을 딱 한 번 수행
- 두 번째도 실패하면 잘못된 답 대신 거절하는 fail-closed 처리

### 01:00 KST — 오탐 반복 수정

- 2024년/24년도, 회의 날짜, 인용 화자 수 같은 정상 표기 차이를 허용
- 경제지표 비교를 여야 비교로 오인한 규칙 분리
- 과학기술혁신본부장을 정부측으로 분류
- 주변 보조 문맥의 750명 수치를 답변이 직접 근거처럼 사용한 문제를 재생성에서 차단

### 01:05 KST — 첫 전체 수정 후 채점

- 20/20 HTTP 성공, 독립 의미 채점 PASS 17 / FAIL 3
- 실제 실패 `dev-012`의 산정 관계 추론과 `dev-003`의 관계 방향 왜곡 확인
- `dev-019`, `dev-020` 등 질문보다 넓은 필수 정답은 루브릭 결함으로 분리

### 01:15 KST — 생성 정확성 규칙 강화

- 답변 기본 추론을 low에서 medium으로 상향
- 명시되지 않은 금액 합계·배분·인과 추론 금지
- `A와의 이견`을 `A 간 이견`으로 바꾸지 않는 관계 방향 보존 규칙 추가
- 표적 재시험에서 `dev-012`, `dev-003` 독립 의미 채점 PASS

### 01:22 KST — 개발 게이트 통과

- 현재 최종 코드·프롬프트로 20문항을 한 실행에서 재수집
- 독립 의미 채점 원시 PASS 16 / FAIL 4
- 동일 기준의 루브릭 결함 4개 제외 후 유효 16/16 PASS, 근거 없는 추가 주장 0건
- 수정 전 14/16(87.5%)에서 수정 후 16/16(100%)로 개선

### 01:25 KST — 비용·전체 회귀 확인

- 현재 공식 단가로 전체 API 지출 재계산: $3.464659 / 승인 $10
- 외부 API를 끄고 프로젝트 전체 테스트 실행: 179 PASS, 경고 2

### 01:27 KST — 새 원문 문항 작성 전송 보류

- 블라인드 답변 가능 90개와 예비 30개를 만들 `author_gold.py`의 1건 시험을 시도
- 새 회의록 원문을 문항 작성 목적으로 보내는 것은 앞선 답변 의미 채점 승인과 목적이 달라 실행 권한이 없음을 확인
- 우회 전송하지 않고 `NEW-012 BLOCKED`로 기록; API 지출은 발생하지 않음

### 01:28 KST — 블라인드 정답표 방어 장치 완료

- 작성 결과의 ID·원문·메타데이터·핵심값을 큐와 대조하는 `validate_authored_gold.py` 구현
- 130개 모두의 명시 승인 없이는 블라인드·예비 파일을 만들 수 없는 `materialize_blind.py` 구현
- 개발·블라인드·예비 원문 누수 0건을 다시 강제하고, 임시 파일 후 교체 방식으로 조립 중단 시 반쪽 파일을 방지

### 01:29 KST — 최종 보수적 게이트와 전체 회귀 완료

- 기존 채점에서 빠졌던 답변 불가 질문의 필수 거절·무인용 검사를 추가
- 100개 결과가 정확히 모두 존재할 때만 정확도 90%·인용 98%·환각 0·거절 100%를 판정하는 `summarize_gate.py` 구현
- 질문 범위인 날짜·위원회 반복 누락을 오답 처리하는 오탐은 제거하고 핵심 숫자만 결정 규칙으로 검사
- 외부 API 키를 비우고 전체 테스트 재실행: **192 PASS, 경고 2**

### 01:32 KST — 답변 불가 대조군의 가짜 성공 원인 제거

- 저장된 10문항이 이전 산출물이라 API 필터에 위원회 정식명칭을 사용한 사실 발견
- 이 상태에서는 발언자가 없어서가 아니라 검색 필터가 틀려 거절할 수 있어 블라인드 증거로 부적합
- 현재 생성기로 검색 약칭을 넣어 다시 만들고, 실제 회의 존재·대상 발언자 0건을 DB에서 재증명

### 01:35 KST — 블라인드 source 독립성 강화

- 답변 불가 대상 회의 1개가 답변 가능 120개 작성 큐와 겹친 사실 발견
- 개발 20개와 답변 가능 120개의 모든 source를 대조군 선택에서 제외하도록 생성기 수정
- 다시 생성한 10개는 개발 source 중복 0, 작성 큐 source 중복 0, DB 부재 증명 PASS

### 01:38 KST — 실행·채점 감사 장치 추가

- 최종 100개 실행의 ID 누락·중복·질문/필터 변경·HTTP 실패·응답 손상을 차단하는 `audit_run.py` 구현
- 의미 채점 입력에서 DB에 없는 인용을 조용히 버리지 않고 hard fail하도록 변경
- 답변 불가 채점에 DB 부재 증명을 전달하도록 변경
- 답변 생성 런타임 파일 6개의 SHA-256과 실제 환경 설정을 실행 config에 기록

### 01:41 KST — 검토·비용·회귀 마감

- 130개 원문 검토 묶음과 4항목 명시 체크리스트 생성기 구현
- 질문 범위·원문 지지·주장 범위·핵심값이 모두 true이고 검토자·시각이 있어야 동결되도록 강제
- 작성 API 사용량을 총비용 장부에 포함하고 작성 $2.50·의미 채점 $2.00 상한 설정
- 현재 작성 API 호출은 0건, 전체 누적 $3.464659 / $10 유지
- 외부 API를 끄고 전체 테스트 재실행: **211 PASS, 경고 2**

### 01:46 KST — 중단 재개 혼합 방지

- 동일 결과 파일 재개 시 데이터셋·프롬프트·모델·추론강도·런타임 코드 해시를 비교하도록 변경
- 하나라도 바뀌면 기존 결과에 이어 쓰지 않고 즉시 실패
- 작성·답변 수집·독립 의미 채점 세 단계 모두에 적용하고 반례 테스트 추가

### 01:49 KST — 외부 호출 전 최종 사전점검

- 개발 20, 작성 큐 120, 대조군 10, ID·source 유일성, 유형 분포, DB 부재 증명, 비용 여유를 통합 검사
- 11개 검사 모두 PASS, 현재 단계 `READY_FOR_AUTHORING_APPROVAL`
- 비용 예약: 작성 $2.50, 의미 채점 $2.00, 예비 3회+최종 답변/재순위 $1.75; 남은 승인액 $6.535341
- 승인 후 전 과정을 그대로 재현할 `RUNBOOK.md` 작성
- 외부 API를 끄고 전체 테스트 재실행: **218 PASS, 경고 2**

### 01:53 KST — 예비 30개 반복 안정성 단계 준비

- 예비 30문항을 세 번 실행한 90회 노출에서 자동 실패·grounding·검증 flag·인용 변동을 집계하는 도구 구현
- 90회 중 자동 실패가 하나라도 있으면 안정성 게이트 FAIL
- 이 단계는 의미 정확도 검사를 대체하지 않는다고 결과에 명시

### 01:56 KST — 비용 누락·런타임 변경 방지 마감

- 작성·의미 채점의 성공/실패 모든 API 호출을 별도 ledger에 먼저 기록하도록 변경
- JSON 파싱 실패도 사용 토큰과 비용을 잃지 않고, 재개 시 실패한 ID만 다시 시도 가능
- 최종 런타임은 `.venv`를 제외한 전체 backend Python과 requirements를 해시로 동결·재검증
- 외부 API를 끄고 전체 테스트 재실행: **224 PASS, 경고 2**

### 01:58 KST — 권한 경계에서 안전 중단

- 블라인드 사전점검 11개는 모두 PASS이고 작성 결과 파일은 여전히 0건임을 재확인
- 새 회의록 원문 120건의 문항 작성 전송 승인이 세 번 연속 확인 단계에서도 제공되지 않음
- 승인 없는 우회 전송·로컬 모델 대체·기존 평가자료 재사용을 하지 않음
- 목표의 남은 단계는 D1-05와 이에 의존하는 D3-01~D3-06이며, 승인 후 RUNBOOK 1번부터 재개 가능

### 08:13 KST — 전송 목적 승인 후 외부 목적지 확인 대기

- 사용자가 블라인드 문항 작성용 새 원문 120건 전송과 총 $10 한도를 승인
- 실행 보안 검토는 승인 문구에 외부 목적지 `OpenAI API`가 명시되지 않아 호출을 차단
- 원문·메타데이터는 전송되지 않았고 `authoring_results*` 파일도 0건, 비용 $3.464659 유지
- 목적지와 모델을 명시한 승인 후 동일한 $2.50 작성 상한으로 재개 예정

### 09:05 KST — 원문 120건 작성·검증 완료

- OpenAI API `gpt-5.6-sol` 목적지와 총 $10 한도 명시 승인 확인
- high/medium 실행의 출력 절단 기록을 보존하고 low 추론으로 120/120 유효 JSON 작성
- 작성 호출 137회(실패·재시도 포함), 작성 비용 $2.358480
- exact_values 18건의 표기·구조 오류를 원문 기준으로 수리하고 120/120 자동검사 PASS
- 원문 자체 수치 모순 `blind-008`, `blind-012`는 원문 기록과 검증 필요성을 함께 답하도록 보수적으로 변경

### 09:11 KST — 골드 조립과 데이터 동결

- 블라인드 100개(답변 가능 90+답변 불가 10), 예비 30개 조립
- 개발·블라인드 source 중복 0
- 검수자는 `Codex independent source review (not human)`으로 기록해 사람 검수를 사칭하지 않음
- dataset manifest와 정답 제거 `blind_questions.jsonl` 해시 동결

### 09:27 KST — 예비 안정성 30문항×3회 FAIL

- 90회 모두 HTTP 200, 결정 규칙 81/90 통과
- 실패 문항 5개: `reserve-009`, `012`, `014`, `016`, `022`
- 주요 원인: 검색 표현 불일치, 법률명 다어절 앵커 부재, `개국의·보고한·차관은`을 인물명으로 오인한 검증 오탐
- RRF 공식은 변경하지 않음

### 09:37 KST — 예비 실패 표적 수정·회귀

- 지방자치단체↔지방광역단체 별칭과 법률명 다어절 키워드 앵커 추가
- 명시 인물 검사는 직함에 실제 인접한 이름만 대상으로 제한
- 실패 5문항×3회 15/15 자동 통과, 추가 영향권 5문항 5/5 통과
- 전체 30×3 새 런타임 재실행은 $10 한도 때문에 생략한 한계를 기록
- backend 19개 파일 해시 동결

### 09:51 KST — 최종 블라인드 100개 단일 실행

- 정답 제거 동결 질문만 API에 전송
- 100/100 HTTP 200, 누락·중복·요청 변경 0
- 실행 후 runtime manifest 검증 변경 0
- grounding 분포 FULL 81 / PARTIAL 9 / REFUSED 10

### 09:53 KST — 최종 하드 게이트 FAIL

- 답변 가능 골드 source 인용 88/90 = 97.78% (기준 98% 미달)
- 답변 불가 무인용 거절 8/10 = 80% (기준 100% 미달)
- 확실한 실제 실패: 정답 있음 거절 2건, 답변 불가에 유사 이름 타인 인용 2건
- 숫자 누락 자동 실패 2건은 `몇백억/수백억`, `열 번/열 차례` 동의 표현 오탐으로 분리
- 하드 기준만으로 최종 FAIL이 확정되어 남은 $0.193을 넘는 의미 채점 API는 호출하지 않음
- 총 API 추정액 $9.806811 / $10

### 09:55 KST — 전체 회귀와 정직한 종료

- 전체 테스트 **229 PASS, 경고 2**
- 최종 결과를 PASS로 포장하지 않고 `FAIL_EARLY`로 기록
- 같은 블라인드 100개는 다시 최종 평가에 재사용하지 않음
- 다음 루프: 실제 실패 4개를 개발셋으로 이동, 새 독립 블라인드 보충, 채점 동의표현 오탐 수정
## 2026-08-18 12:48 KST — 2차 로컬 개선 루프

- 최종 실패 4건의 저장 답변·원문·검증 상세를 대조했다.
- `blind-024·074`: 검색 성공 후 일반어를 인물로 오인한 검증기 차단으로 확인했다.
- `blind-093·098`: 질문 대상과 비슷한 이름의 타 화자 인용으로 확인했다.
- 실패 4건을 `dev_regression_round2.jsonl`의 `dev-101~104`로 승격하고 스키마 검사 PASS, SHA-256 `6fec950e`를 기록했다.
- 활용형 인물 오탐, 특정 화자 부재 사전차단, 수량 동의표현 정규화를 구현했다.
- 기존 답변 사후 재채점에서 AUTO_FAIL 6→4로 감소했으며 이는 새 품질 점수가 아니다.
- 관련 테스트 58 PASS, 전체 테스트 234 PASS(경고 2, 실패 0).
- 외부 API 호출과 비용 증가는 없으며 RRF도 수정하지 않았다.

### 13:51 KST — 2차 신규 블라인드 실행과 하드 게이트

- 사용자가 신규 회의록 원문·평가 답변의 OpenAI API 전송과 추가 $6 한도를 승인했다.
- 이전 최종 블라인드 source를 전부 제외하고 신규 source pool 260건에서 답변 가능 120건과 부정문항 10건을 구성했다.
- `gpt-5.6-sol`로 골드 120건을 작성한 뒤 전부 원문 대조했다. exact value 14건, 모호한 질문 3건, 구조화 출력 특수문자 오염을 원문만으로 수리했다.
- 120/120 골드 자동검증, 130건 Codex 독립 원문검토 승인(사람 검수 아님), 블라인드 100+예비 30 동결을 완료했다.
- 전체 테스트 240 PASS, API/DB 정상, 런타임 19개 파일 해시를 실행 전후 검증했다.
- 새 블라인드 100/100 HTTP 200, 누락·중복·요청 변경 0으로 실행 감사 PASS였다.
- 하드 게이트: 답변 가능 인용 85/90(94.44%), 답변 불가 거절 10/10, 자동 실패 9건으로 `FAIL_EARLY`.
- 실제 거절 실패는 `blind-040·047·069·077·084`, 필수 숫자 누락은 `blind-009·022·076`이다.
- `blind-049`는 답변의 `3대`와 골드의 `세 대`를 채점기가 다르게 본 오탐이지만, 이를 제외해도 인용 98% 기준은 실패한다.
- 하드 실패가 확정되어 유료 의미 채점은 실행하지 않았다. 추가 지출 $3.766778 / $6, 잔액 $2.233222.
- RRF·UI·배포·Git commit/push는 변경하지 않았다.

### 14:34 KST — 포트폴리오 기준 재평가

- 운영급 기준을 덮어쓰지 않고 별도 `portfolio` 프로필을 추가했다.
- 기준은 전체 85%, 인용 90%, 답변 불가 거절 90%, 근거 없는 주장 0건으로 사전 고정했다.
- 저장 답변 100건과 골드·실제 인용 전문 133개를 `gpt-5.6-sol` high로 독립 의미채점했다.
- JSON 형식 실패 2회는 결과에 섞지 않고 비용 ledger에 기록한 뒤 해당 ID만 재시도했다.
- 의미판정은 PASS 84·FAIL 15·REVIEW 1, 규칙 결합 최종 점수는 82점이었다.
- 인용 92.22%와 거절 100%는 통과했지만 전체 85%와 근거 없는 주장 0건 기준을 실패했다.
- 근거 없는 주장/치명 오류 8건에는 잘못된 거절 5건, 인용에 없는 기관명 확장, 금지 수치 제시 등이 포함됐다.
- 이름이 다른 의미채점 파일 비용 누락을 발견해 집계 glob을 일반화하고 반례 테스트를 추가했다.
- 최종 추가 지출 $5.532318 / $6, 잔액 $0.467682. 한도 내에서 종료했다.

### 16:16 KST — QA-CYCLE-03 원인 분리와 보수적 채점 개편

- 잘못된 거절 5건은 검색 실패가 아니라 검증기의 인명 오탐으로 확정했다. 5건 모두 gold source가 Top 5에 있었다.
- 인명 후보를 한국 성씨 구조로 제한하고 실제 미지원 인물 반례를 보존했다.
- `세 대↔3대`를 동치 처리하되 `세부·세계`를 숫자로 오인하지 않도록 채점기를 수정했다.
- 신규 골드는 필수값과 참고값을 분리하고 자동 누락 실패는 필수 숫자에만 적용하도록 생성·스키마·채점·의미평가 입력을 연결했다.
- 같은 회의록의 다른 chunk를 gold 인용으로 통과시키던 결함을 수정해 정확한 gold chunk 일치를 요구했다. 진단 재채점에서 `blind-021`을 새로 검출했다.
- 원문의 `공단`을 `국가철도공단`으로 확장하는 기관명 환각을 품질게이트에서 차단하고, 질문 밖 배경·수치를 추가하지 않도록 생성 지시를 강화했다.
- 코드의 느슨한 portfolio 기준 85/90/90을 고정 목표 90/95/100/0으로 상향했다.
- 외부 API 호출 없이 기존 100건을 진단 재채점했고 AUTO_FAIL 9건을 확인했다. 이는 폐기된 블라인드의 진단값이며 새 최종 점수가 아니다.
- 관련 테스트 100 PASS, 전체 테스트 **258 PASS**, 경고 2, 실패 0. RRF 공식은 변경하지 않았다.
- 미해결: `blind-021` gold chunk의 검색 단계 탈락과 pre-RRF/RRF/post-rerank trace 부재. 신규 E2E 및 새 블라인드 비용도 아직 집행하지 않았다.

### 16:28 KST — 검색 trace와 G2 자동 판정 구현

- RRF 공식·가중치·정렬은 바꾸지 않고 keyword 후보, vector 후보, RRF 상위, 최종 답변 문맥의 chunk 순위를 평가 요청에만 저장하도록 구현했다.
- 일반 UI 요청에는 trace를 노출하지 않고 `run_baseline.py`의 평가 요청만 `include_trace=true`를 사용한다.
- 실행 감사는 trace 누락·형식 오류를 하드 실패로 처리한다.
- `analyze_retrieval_trace.py`가 RRF Recall@10 95%, 최종 문맥 Recall@5 95%, 유형별 RRF Recall@10 85%, 필터 오류 0을 자동 판정한다.
- RUNBOOK의 예비 3회와 최종 블라인드 순서에 trace 감사·분석을 추가했다.
- 단계별 순위 기록이 최종 검색 결과 순서를 바꾸지 않는 반례 테스트를 포함해 전체 **263 PASS**, 경고 2, 실패 0.
- 다음 단계: 현재 변경으로 신규 개발 E2E를 실행해 QA-0001·0002 재발과 실제 G2 지표를 확인한다. 외부 API 비용은 별도 승인 전 사용하지 않는다.

### 16:34 KST — 골드 자동 일괄 승인 경로 차단

- 자동 구조 검사 PASS만으로 130개를 `Codex source review` 승인 처리할 수 있던 경로를 재현했다.
- 골드 검토 문서에 답변 필수값과 원문 참고값을 따로 표시하고, 승인 체크도 두 역할을 분리했다.
- 최종 승인에는 별도 문항별 완료 검토 파일의 130개 ID, approved 상태, 5개 체크, 검토자, 시각, 구체 메모를 모두 요구한다.
- pending·누락 검토와 구형 단일 exact-value 체크는 승인되지 않는다.
- 승인·검토 관련 테스트 18 PASS. 다음 신규 골드에서 실제 검토 완료 파일과 해시 생성 확인이 남았다.

### 16:40 KST — UI·평가 입력 동등성과 평가 런타임 동결

- 프론트는 `question+mode`만 보내지만 기존 평가기는 gold 날짜·위원회 필터까지 직접 넣어 평가가 더 유리했음을 확인했다.
- 평가 요청도 프론트와 동일한 의미 payload로 변경했다. 관측용 `include_trace`만 추가되며 검색 힌트로 사용되지 않는다.
- 질문 파서가 추출한 날짜·위원회가 gold와 일치하는지는 G2 trace 분석에서 하드 판정한다.
- 이전 82점은 필터 주입 조건의 과거 진단값이므로 현재 제품 성능 점수로 재사용하지 않는다.
- backend 전체뿐 아니라 모든 loop v2 평가 Python과 `frontend/src/api.js`를 runtime manifest 해시에 포함했다.
- UI·평가 route/payload 동등성 및 runtime hash 관련 테스트 12 PASS.
- 변경 전체 회귀 **269 PASS**, 경고 2, 실패 0과 `git diff --check` 통과.
- 새 G2 분석기로 과거 2차 블라인드를 재검사하자 trace 90/90 누락으로 예상대로 `FAIL`했다. 따라서 과거 82점을 새 최종 결과로 재사용할 수 없다.

### 16:49 KST — 질문 날짜·위원회 파서 전수검사와 P0 수정

- 외부 API를 호출하지 않고 2차 블라인드 100건과 예비 30건의 질문 파서 결과를 gold 필터와 비교했다.
- 블라인드 9건·예비 2건에서 회의 날짜와 질문 본문의 참고 날짜·연도를 하나의 넓은 범위로 합치는 오류를 확인했다.
- `맨 앞 날짜 + 위원회` 형식의 첫 날짜만 회의 필터로 쓰고 본문 날짜는 검색어에 보존하도록 수정했다. 명시 기간 질문의 기존 동작은 유지했다.
- 수정 후 블라인드 100/100, 예비 30/30, 2차 회귀 4/4 필터 일치. 구형 dev는 6/20만 일치해 신규 점수용으로 부적합함을 기록했다.

### 16:56 KST — 평가 동결 게이트·숫자 검증·완전성 보강

- 새 동결 과정은 dev·reserve·blind 모든 질문에서 파서 필터와 gold 필터가 정확히 같아야 통과한다. 질문에 숨은 필터를 다시 주입하는 데이터는 동결할 수 없다.
- 숫자 검증 반례에서 `97년→1997년` 정상 표기를 근거 없는 값으로 오판하는 결함을 재현하고, 1900·2000년대 축약 연도 동치로 수정했다.
- `여 개·대·주·꼭지` 단위를 숫자 주장 검사에 포함해 `2,500여 개·13대·4주` 환각 사각지대를 차단했다.
- 치명 오류 재생성은 검출된 값만 삭제하고 질문에 필요한 근거 수치·연도·기간을 보존하도록 보강했다.
- 과거 의미채점 중 질문 밖 세부사항을 강제한 `blind-012·065·079`를 골드 검토 결함으로 분류했다. 신규 문항별 `claims_match_question` 검토 없이는 승인되지 않는다.
- 관련 72 PASS, 당시 전체 **275 PASS**, 경고 2, 실패 0. 외부 API 비용 사용 0.

### 17:03 KST — 최종 90점 게이트 우회 경로 차단

- exact gold chunk 값이 빠져도 같은 source 인용으로 통과하던 legacy fallback을 최종 점수에서 제거했다.
- 최종 CLI는 100문항의 답변 불가 구성을 정확히 10건으로 강제한다. 99/1처럼 쉬운 구성으로 거절 100%를 주장할 수 없다.
- 하드게이트도 dataset·deterministic 중복 ID를 명시적으로 거부한다.
- source-only 인용, 잘못된 90/10 구성, 중복 ID 반례 포함 13 PASS. 전체 **279 PASS**, 경고 2, 실패 0.

### 17:18 KST — 답변 불가 필터와 실제 인용 문맥 정렬

- G2 trace·필터 검사를 answerable 90건에서 전체 100건으로 확대했다. 잘못된 필터로 검색 0건이 된 부정문항은 거절 성공으로 인정하지 않는다.
- health의 988건 임베딩 차이는 stale 근사 통계였고 정확 집계는 chunks=420,004, embeddings=420,004, missing=0이었다. 응답에 근사값 표시를 추가했다.
- 답변 모델이 같은 turn의 여러 청크를 복원해 보면서도 사용자·의미채점은 대표 hit 한 청크만 보던 불일치를 확인했다.
- 모델이 실제 본 `support_chunk_ids`를 citation과 trace에 기록하고, 사용자 citation API·독립 의미채점·결정 채점·최종 문맥 recall을 같은 근거 범위로 맞췄다.
- RRF 계산식·가중치·정렬은 변경하지 않았다. 관련 57 PASS, 전체 **284 PASS**, 경고 2, 실패 0. 외부 API 비용 사용 0.
- 현재 코드 임시 서버(8001)에서 `blind-021` 대표 citation 001을 조회해 support ID가 001+002이고, 반환 전문에 정답 의안번호 `2210866·2208063`이 모두 포함됨을 확인한 뒤 서버를 종료했다.

### 17:37 KST — round3 독립 원문 대표성 보강과 개발·부정 큐 준비

- 최초 round3 360건은 균형 샘플 뒤 위험 원문 164건을 버려 안전 기재위가 3건뿐이고, 개발용 잔여에는 기재위·국방위·외통위가 0건인 선택 순서 결함을 확인했다.
- DB 후보 23,648건에서 문맥 의존·절차·잘린 발언을 먼저 제거했다. 안전 후보 11,759건에서 source 고유 360건을 다시 뽑았고 보고서 경보는 0건이다.
- 작성 큐가 위원회별 원문을 개발용으로 최소 2건 남기도록 수정했다. 최종 작성 큐는 blind 90·reserve 30이며 blind에 9개 위원회가 모두 포함되고 기재위는 5건이다.
- 별도 dev 작성 큐 18건은 4개 유형 5/4/4/5, 9개 위원회 각 2건으로 구성했다.
- 답변 불가 blind 10건과 dev 2건을 신규 회의·발언자 조합으로 만들고, 12건 모두 독립 SQL `COUNT(*)=0`을 증명했다.
- 답변 가능 source 138개는 모두 고유하며 답변 불가 target source 12개와 중복 0, dev↔blind source 중복도 0이다.
- 주요 해시: 후보 풀 `9ab90d5c...`, blind/reserve 큐 `3e490443...`, dev 큐 `87a79d65...`.
- 전체 테스트 **289 PASS**, 경고 2, 실패 0. `git diff --check`는 공백 오류 없이 줄바꿈 경고만 있었다. 외부 API 호출·비용 사용 0, RRF 변경 0.
- RUNBOOK 최종 게이트 명령이 기본 strict 98%를 사용하던 불일치를 발견해 `--profile portfolio`를 명시하고 고정 목표 90/95/100/0과 일치시켰다.
- 작성기의 증분 비용 상한이 과거 ledger까지 재차 차감하는 결함을 수정했다. E2E 실행별 비용 상한도 추가하고, round3 전체 사전예약은 모든 단계와 오버슛을 포함해 **$10**로 고정했다.

### 17:48 KST — round3 로컬 준비 최종 회귀

- source 선제 필터·독립 split·부정 대조군·작성/E2E 비용 상한 변경을 포함한 전체 테스트 **291 PASS**, 경고 2, 실패 0.
- `git diff --check`는 공백 오류 0이며 기존 Windows 줄바꿈 경고만 남았다.
- 신규 외부 API는 호출하지 않았고 승인 잔액은 $0.467682로 유지된다.

### 18:01 KST — round3 실행 경로 완결성 감사

- 블라인드 130건 전용으로 고정된 검토·승인·조립 도구 때문에 신규 dev 18+2를 실제 G5 입력으로 만들 수 없는 결함을 확인했다.
- 검토·승인 도구를 기대 개수 매개변수로 일반화하고 exact dev ID·20건 승인·2건 부정 증명을 강제하는 `materialize_dev.py`를 추가했다.
- RUNBOOK을 round3 실제 경로로 전면 연결하고 dev G5 E2E·독립 의미채점 명령을 추가했다.
- 예비 3회 뒤에 런타임을 동결하던 순서를 G5 직후 동결·각 실행 전 verify로 수정했다. 세 실행 config/runtime hash가 다르면 안정성 집계가 실패한다.
- 실제 `source_preflight.json`은 source·ID·유형·9개 위원회·SQL 부정 증명·dev/blind/reserve 20/100/30 고유 source 검사를 모두 통과했다. 현재 상태는 잔액 $0.467682 때문에 `WAITING_FOR_BUDGET`이다.
- 이 단계의 외부 API 호출과 비용 증가는 0이며 RRF는 변경하지 않았다.
- 전체 회귀 **297 PASS**, 경고 2, 실패 0. `git diff --check` 공백 오류 0.

### 18:10 KST — G3 생성 단독 실행 경로 구현

- QA 계획의 G3가 실행 명령 없이 문서에만 있음을 확인했다. 제품 생성기는 hits를 직접 받아도 이웃 turn과 비교 이슈 집계를 추가해 gold-only 시험이 아니었다.
- `gold_context_only` 옵션과 `run_gold_context.py`를 추가해 검색·이웃·이슈 주입 없이 검토된 gold turn만 전달하고, 정답·필수 주장은 모델 입력에서 제외했다.
- answerable-only 18건을 위한 generation 게이트를 추가했다. 의미·인용 95%, 잘못된 거절 0, 근거 없는 주장 0을 요구하므로 실제로는 18/18이어야 통과한다.
- 과거 실측 평균과 20% 여유로 round3 전체를 재산정하니 약 $10.54여서 승인 요청 상한을 **$11**로 수정했다. 외부 API 호출은 0이다.

### 18:18 KST — G4 실제 답변 기반 검증기 보정

- 폐기된 dev·round2 답변과 기존 독립 의미판정을 최종 점수가 아닌 검증 보정셋으로 재생했다.
- 최초 정상 오차단은 8/84(9.52%)였으며, 원래 인용 번호 유실로 인한 측정 오탐 7건과 `세 대→3대` 검증 오탐 1건을 분리했다.
- 보정 대상을 99개 정상 답변으로 늘리자 기관 가운뎃점 열거와 `번↔회`에서 2건 오차단(2.02%)이 추가로 드러났다.
- citation n 보존, 고유어 수사·번/회 동치, 기관 열거 분리, 장문 의안번호 검증을 일반 규칙으로 수정했다.
- 최종 G4는 정상 오차단 **0/99(0%)**, 근거 없는 구체 주장 차단 **4/4(100%)**로 PASS했다. 잘못된 거절 5건과 질문 범위 위반 1건은 G3/G5 의미판정으로 별도 관리한다.
- 외부 API 호출·비용 증가는 0이고 RRF는 변경하지 않았다.
- 전체 회귀 **302 PASS**, 경고 2, 실패 0. `git diff --check` 공백 오류 0.

### 19:05 KST — 유료 실행 직전 재시작·G3 동등성 감사

- 유료 호출 전 전체 회귀를 재실행해 기존 코드 기준 302 PASS를 확인했다.
- 실패한 API 행도 ID 존재만으로 완료 처리해 재실행에서 건너뛰는 P0 결함을 확인했다.
- 성공 결과는 `results.jsonl`, 실패 시도는 `errors.jsonl`로 분리하고 두 원장의 usage를 비용 상한에 모두 포함했다. 기존 결과 원장에 실패행·중복 ID가 있으면 새 실행 폴더를 요구한다.
- G3 직접 생성 경로가 제품 `/query`의 grounding 판정과 검증 flag 강등을 건너뛰는 P0 동등성 결함을 확인해 동일 후처리를 적용했다.
- 추가 반례 4개를 포함한 전체 회귀 **306 PASS**, 경고 2, 실패 0.
- `.env`의 API 키 존재는 값 노출 없이 확인했다. 신규 외부 API 호출·비용 증가는 0이며, round3 추가 비용 상한 $11의 명시 승인은 아직 받지 않았다.

### 19:12 KST — round3 외부 평가 승인

- 사용자 승인 원문: `round3 평가 자료 전송과 OpenAI API 추가 비용 상한 $11을 승인한다.`
- 승인 범위: round3 신규 원문·개발/G3/예비/블라인드 평가 자료의 OpenAI API 전송과 이번 회차 누적 추가 비용 최대 $11.
- 실행 규칙: 단계별 원장 비용을 합산하고, 각 게이트 실패 또는 승인 상한 도달 시 다음 단계로 진행하지 않는다.

### 21:24 KST — round3 G3·G5 개발 게이트 완료

- 신규 gold 130건을 원문과 독립 대조해 자동검사가 놓친 8건을 재작성했고, dev 20·blind 100·reserve 30 데이터 동결을 완료했다.
- G3 반복 실패에서 명시 시점 누락, 평가 표현 추론, 시각 조사 변경을 발견해 일반 검증·1회 재생성 규칙으로 수정했다.
- 실제 G5 실패에서 QA 주변 턴의 인용 오염과 질문 전제 누락을 확인해 QA 주변 턴 차단·최소 근거·질문행위 가드를 적용했다.
- 최종 코드 기준 `g3-gold-context-v6`은 18/18, 의미·인용 100%, 치명 환각 0으로 PASS했다.
- 실제 UI 의미 입력과 같은 `/query` 경로 `dev-g5-v3`은 20/20 HTTP 200, 의미 20/20, 인용 18/18, 답변 불가 거절 2/2, 치명 환각 0으로 portfolio 게이트 PASS했다.
- G2 검색은 keyword@30 100%, final context@5 100%지만 RRF@10 16/18(88.89%)로 95% 기준 FAIL이다. `dev-012·014`가 RRF 10위 밖이었다.
- 사용자 지시에 따라 RRF 수식·가중치·정렬은 변경하지 않았고, 기준도 사후 하향하지 않았다. 이 때문에 런타임 동결·예비 3회·최종 블라인드는 시작하지 않았다.
- 전체 회귀 **330 PASS**, 경고 2, 실패 0. `git diff --check` 공백 오류 0.
- round3 저장 원장 공식 단가 재집계: **$6.613076 / $11**, 잔액 **$4.386924**.

### 2026-08-19 — G2 포트폴리오 검색 기준 확정

- 사용자가 RRF Recall 목표 95%와 final Context Recall 95%가 포트폴리오에 과도하다고 판단해 두 전체 합격선을 85%로 확정했다.
- 소규모 유형 묶음은 4~5건이라 한 건 실패 영향이 20~25%인 점을 반영해 유형별 RRF 하한은 75%, 날짜·위원회 필터 오류는 계속 0건으로 유지한다.
- 기존 `dev-g5-v3`의 95% 기준 FAIL과 16/18(88.89%) 수치는 삭제하지 않는다. 변경은 G6·G7 전에 문서·코드·반례에 먼저 고정하며 RRF 공식·가중치·정렬은 바꾸지 않는다.

### 2026-08-19 — RRF 분석 및 G3·G5 재검사 완료

- `dev-012·014`는 keyword 1위지만 vector 목록에 없어 표준 weighted RRF에서 16·30위로 내려갔고, reranker가 둘 다 최종 1위로 회복했다.
- keyword 가중치 3.5에서는 RRF 18/18이 되지만 한 검색축에 과도하게 의존하는 개발셋 맞춤 변경이라 제품 RRF 수식·가중치·정렬은 변경하지 않았다.
- 새 기준 재집계: RRF Recall@10 16/18(88.89%), final Context Recall@5 18/18(100%), 유형별 75% 이상, 필터 오류 0으로 G2 PASS.
- G3 `g3-gold-context-v7`: 18/18 PASS, 의미·인용 100%, 치명 환각 0. 생성 $0.1299, 독립 채점 $0.2526.
- G5 `dev-g5-v4`: 20/20 HTTP 200, 감사·검색 게이트 PASS. 답변/재순위 $0.3418.
- 최초 의미채점 19/20에서 `dev-019`가 거절문에 질문 메타데이터를 반복하지 않았다는 이유로 잘못 실패했다. 채점 입력 fallback을 일반 수정하고 old judge 결과는 보존했다.
- 새 `judge_inputs_v2.jsonl`과 `semantic_judgments_v2.jsonl` 재채점은 20/20 PASS, 의미 100%, 인용 100%, 거절 100%, 치명 환각 0. 재채점 비용 $0.2987.
- loop v2 150 PASS 후 백엔드를 포함한 전체 회귀 **332 PASS**, 실패 0(환경 경고 2). round3 누적 비용 **$7.948607 / $11**, 잔액 **$3.051393**.
- 평가 중에만 완화했던 API 비용·분당 제한을 제거하고 기본 설정으로 재시작했다. `/health`는 `ok`다.

### 2026-08-19 — 예비 1회 및 QA-CYCLE-03 최종 블라인드 완료

- 사용자 결정으로 예비 30문항×3회를 1회로 축소했다. 예비는 30/30 HTTP 200, RRF 96.67%, final Context 100%, 필터 오류 0이었다.
- 예비 규칙 실패 3건은 숫자·구성합계·한자 화자 별칭 오탐으로 확인해 일반 수정했고 관련 17 PASS, 저장 답변 재채점 AUTO_FAIL 0을 확인했다.
- 최종 runtime을 `runtime_manifest_final.json`로 별도 동결하고 정답 제거 질문으로 블라인드 100문항을 한 번만 실행했다.
- 실행 감사 100/100 PASS. 생성 비용 $1.8942, HTTP 실패 0.
- RUNBOOK의 `blind_gold.jsonl` 오기가 manifest의 실제 `blind.jsonl`과 달라 로컬 채점이 한 번 중단됐다. API 결과는 보존했고 문서 경로만 수정했다.
- 검색은 keyword 100%, vector 92.22%, RRF@10 96.67%, final Context@5 100%, 유형별 RRF 95% 이상, 필터 오류 0으로 PASS했다.
- 규칙 하드게이트는 인용 97.78%, 거절 10/10으로 의미채점 진입을 허용했다.
- sol high 독립 의미채점은 JSON 오류 1회를 비용 원장에 남기고 동일 config로 재개해 100/100 완료했다.
- 최종 게이트는 정확도 81%, 인용 96.67%, 거절 100%, 치명 오류 4건으로 **FAIL**했다.
- round3 누적 비용은 **$12.261904 / $12.50**, 잔액 **$0.238096**이다.
- 최종 전체 회귀 **335 PASS**, 실패 0(환경 경고 2), `git diff --check` 통과.
- 평가 중에만 완화한 API 제한을 제거해 기본 설정으로 재시작했고 `/health` status·DB 모두 `ok`다.

### 2026-08-19 — 최종 81점 고정 및 치명 오류 4건 사후 보완

- 사용자가 추가 블라인드 검사를 종료하고 공식 정확도를 81점으로 유지하기로 결정했다.
- `blind-005`: 근거 숫자 9000·13500의 직접 차이 4500을 환각으로 오인하던 수치 검증을 같은 계수사·명시값 쌍에 한해 산술 검증하도록 수정했다.
- `blind-016`: `전날 국방부차관`에서 상대 시점 표현을 사람 이름으로 오인하지 않도록 제외했다.
- `blind-010`: `6조 4800`의 생략 단위 동치와 전후값 질문의 시작·종점 보존 가드를 추가했다.
- `blind-059`: 정당과 무관한 질문에는 source 당적을 모델에 주지 않고 불필요한 당적 접두를 제거한다.
- 대상 회귀 62 PASS, 전체 회귀 **336 PASS**, 실패 0. 외부 API 호출·비용 증가는 0.
- 공식 최종 점수는 재산정하지 않았으며 포트폴리오 문서에 이 한계를 명시했다.
